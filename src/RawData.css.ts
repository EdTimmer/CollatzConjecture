import styled from 'styled-components';

export const DataContainer = styled.div`
  width: 100vw;
  padding: 30px;
  display: flex;
  flex-direction: column;
`;

export const TextLabel = styled.p`
  font-size: 22px;
  margin: 0 10px 0 0;
`;

export const DataValues = styled.h3`
  margin: 0;
  padding: 0;
  font-size: 32px;
`